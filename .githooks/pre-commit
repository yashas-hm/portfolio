#!/usr/bin/env bash
set -euo pipefail

staged_files=$(git diff --cached --name-only --diff-filter=ACM -- '*.dart')

if [ -z "$staged_files" ]; then
  exit 0
fi

dart format $staged_files

# Re-stage any files that dart format may have modified
echo "$staged_files" | xargs git add
